#pragma once

#include <algorithm>
#include <iterator>

namespace whole
{
// TODO
// template< class Forward >
// constexpr auto shift_left( Forward &one, typename std::iterator_traits<typename Forward::iterator>::difference_type n)
// {
//     return std::shift_left(std::begin(one), std::end(one), n);
// }

// template< class Forward >
// constexpr auto shift_right( Forward &one, typename std::iterator_traits<typename Forward::iterator>::difference_type n)
// {
//     return std::shift_right(std::begin(one), std::end(one), n);
// }
}
